<resources>

    <!-- Main activity theme. -->
    <style name="PopularMoviesTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Use the following item when using Theme.AppCompat.SomethingElse without .NoActionBar-->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Black Text, White Activity Background-->
    <style name="PopularMoviesToolbarTheme" parent="Theme.AppCompat.NoActionBar">
        <!--<style name="AppTheme" parent="Theme.AppCompat.NoActionBar">-->

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <!--<item name="colorAccent">@color/colorAccent</item>-->

        <!-- android:textColorPrimary is the  color of the title text
             in the Toolbar, in the Theme.AppCompat theme:  -->
        <!--<item name="android:textColorPrimary">@color/abc_primary_text_material_light</item>-->

        <!-- android:textColorPrimaryInverse is the  color of the title
             text in the Toolbar, in the Theme.AppCompat.Light theme:  -->
        <!-- <item name="android:textColorPrimaryInverse">@color/abc_primary_text_material_light</item> -->

        <!-- android:actionMenuTextColor is the color of the text of
              action (menu) items in the Toolbar, at least in the
              Theme.AppCompat theme.
              For some reason, they already get the textColorPrimary
              when running on API 21, but not on older versions of
              Android, so this is only necessary to support older
              Android versions.-->
        <!--<item name="actionMenuTextColor">@color/abc_primary_text_material_light</item>-->
        <!-- android:textColorSecondary is the color of the menu
             overflow icon (three vertical dots) -->
        <!--<item name="android:textColorSecondary">@color/abc_secondary_text_material_light</item>-->

        <!-- This would set the toolbar's background color,
              but setting this also changes the popup menu's background,
              even if we define popupTheme for our <Toolbar> -->
        <!-- <item name="android:background">@color/color_primary</item> -->

        <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight, and colorSwitchThumbNormal. -->
        <!--useful link http://www.murrayc.com/permalink/2014/10/28/android-changing-the-toolbars-text-color-and-overflow-icon-color/-->
    </style>

    <!--
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />-->

    <style name="PopularMoviesListStyle">
        <item name="android:choiceMode">singleChoice</item>
    </style>
    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="PopularMoviesTheme">
    </style>


</resources>
